define(['common/tool', 'bs/tab'], function (Tool) {
    var appPage = {};
    // var appId = Tool.getParams("app_id");
    appPage.init = function () {
        // $('#appDetailTab a:last').tab('show');
    };

    return appPage;
}); 